# Harmony

- Harmony is a project I started as a challenge to build an asynchronous HTTP web server.
- The architecture was inspired by Boost.Asio, which maintains an `io_context` object that runs on each thread.

##### Technologies used:

- C++
- C++20 Coroutines
- Linux 5.1 io_uring
- Linux Berkeley sockets.

